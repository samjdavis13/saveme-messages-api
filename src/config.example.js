/**
* @config Application configuration
*/

// Port the app wil run on
module.exports.port = 3000;

// Credentials used for basic http auth
module.exports.user = {
    name: "test",
    pass: "test"
}